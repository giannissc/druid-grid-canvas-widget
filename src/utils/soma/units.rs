pub enum Time {
    Second,
    Millisecond,
    Microsecond,
}

pub enum Distance {
    Meter,
    Centimeter,
    Millimeter,
    Micrometer,
}

pub enum Power {
    Watt,
    Milliwatt,
}

pub enum Effort {
    Pascal,
}

pub enum Flow {
    GramPerSecond,
}

pub enum Resistance {
    Pascal,
}

pub enum Capacitance {
    Meter3PerPascal,
}
